[project]
name = "cnode-platform"
version = "0.1.0"
description = "Plugin-Framework der cNode-Plattform (Connectoren, Formate, Agenten, Systeme)"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["cnode_platform"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
